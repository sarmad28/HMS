server:
  port: 9093


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rating
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect